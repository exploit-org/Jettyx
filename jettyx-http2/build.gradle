plugins {
    id 'java'
}

group = 'org.exploit'
version = '0.1'

repositories {
    mavenCentral()
}

ext {
    projectName = 'Jettyx HTTP2 Support'
    projectDescription = 'Adds support for HTTP/2 communication using Jetty.'
}

dependencies {
    testImplementation platform('org.junit:junit-bom:5.9.1')
    testImplementation 'org.junit.jupiter:junit-jupiter'

    compileOnly(project(":"))
    implementation 'org.eclipse.jetty.http2:http2-client:11.0.24'
    implementation 'org.eclipse.jetty.http2:http2-http-client-transport:11.0.24'
}

jar {
    setDuplicatesStrategy(DuplicatesStrategy.EXCLUDE)

    from(configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }) {
        exclude 'META-INF/*.RSA', 'META-INF/*.SF', 'META-INF/*.DSA'
    }
}

test {
    useJUnitPlatform()
}